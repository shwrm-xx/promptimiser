.DS_Store
node_modules/
*.log

# Artéfacts de test en bac à sable
/.sandbox/

# Plugin Claude Code assemblé (build-plugin.js) — dérivé de la source, jamais committé
/dist/
*.pmz-backup-*.json
*.vsg-backup-*.json

# État PMZ éphémère (réécrit à chaque fin de tour) — handoff.md et, en vague parallèle,
# un handoff par lot tenu par une session fille
.vibe-agent/handoff.md
.vibe-agent/handoff-lot-*.md

# Tableau de bord d'économie de contexte (scripts/dashboard.js) — page régénérable à la
# demande, propre à un poste et à ses transcripts, jamais committée
.vibe-agent/dashboard*.html

# Dépôt git imbriqué (checkout séparé), hors périmètre de ce dépôt
/pmz/
