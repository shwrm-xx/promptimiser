<!-- pmz:archive:lot 103 -->
# Lot #103 — Trailer PMZ-Jira + pointeurs dans la fiche d'archive

- epic : US & Jira · clos : 2026-07-28 · commit : 53e1d31 · session : inconnue
- verify : `node test/run-tests.js` → OK · coût : non mesuré

## Objectif

Troisième lot de l'epic « US & Jira » : faire remonter les pointeurs `us` (#101) et
`integrations.jira.key` (#102) jusqu'aux deux surfaces de clôture qui les ignoraient encore —
le trailer git du commit de clôture et la fiche d'archive tier 1.

## Décisions (et pourquoi)

- Trailer `PMZ-Jira` ajouté au bloc existant (`PMZ-Lot`/`PMZ-Cost`/`PMZ-Model`, lot #60), même
  patron, jamais un nouveau bloc — cohérence avec `git log --format=%(trailers)`.
- `ficheSkeleton` (lib/archive.js) : jira/us ajoutés en en-tête (ligne méta) ET en section
  « Liens » (deux surfaces demandées par le scope), tous deux strictement conditionnels — un
  lot sans l'un ou l'autre ne produit aucune ligne, jamais de `null` ni de valeur inventée.
- Bug trouvé en écrivant les tests d'intégration : `audit-batch.js#backlogSummary().current`
  (la projection du lot courant consommée par close-batch) ne portait ni `epic` ni `us` du lot
  réel — hérité du lot #95, jamais remarqué faute de test dédié sur ce champ précis (la ligne
  méta affichait toujours `epic : —`, silencieusement). Corrigé dans le même périmètre.

## Vérifié

- `node test/run-tests.js` → 1668 OK / 0 échec (dont +7 assertions dédiées : ficheSkeleton
  avec/sans jira+us, trailer avec/sans clé, intégration close-batch de bout en bout).
- Comportement réel vérifié en bac à sable (close-batch avec/sans lot en cours, avec/sans clé
  Jira posée, sortie du squelette de fiche inspectée).
- Canal plugin reconstruit en réel (`build-plugin.js`).

## Non vérifié

- Aucun commit réel n'a encore collé le trailer `PMZ-Jira` en pied de message (usage
  applicatif au-delà du test).

## Dette restante

Aucune propre au lot #103. Reste #104 (doc : ARCHITECTURE/README/CHANGELOG, épic « US & Jira »).

## Périmètre
(globs / fichiers touchés — noms seulement, jamais de diff)

- promptimizer/lib/archive.js
- promptimizer/scripts/close-batch.js
- promptimizer/scripts/audit-batch.js
- test/run-tests.js
- CHANGELOG.md
- ARCHITECTURE.md

## Liens
- commit 53e1d31 · entrée CHANGELOG du 2026-07-28 · brut : archive/raw/lot-0103.md (local)
