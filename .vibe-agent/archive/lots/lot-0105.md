<!-- pmz:archive:lot 105 -->
# Lot #105 — Retrait du pointeur Jira

- epic : Formalisation US · clos : 2026-07-28 · commit : 9444709 · session : inconnue
- verify : `node test/run-tests.js` → OK · coût : non mesuré

## Objectif

Retirer le pointeur `integrations.jira` (posé lots #102-#103, epic « US & Jira ») : sans
connectivité Jira réelle derrière, un simple pointeur de traçabilité ne rendait pas assez
de service pour justifier son coût de doc/code à relire. Garder le pointeur `us` intact.

## Décisions (et pourquoi)

- Retrait chirurgical plutôt que `git revert` : les lots #101-#104 entrelacent US et Jira dans
  les mêmes fichiers (`lib/backlog.js`, `lib/archive.js`, `close-batch.js`). Un revert aurait
  aussi défait le pointeur `us`, qui reste voulu.
- `integrations` ne porte plus qu'un seul sous-champ (`command_optimizer`) : la logique
  défensive de préservation « ne jamais écraser le sous-champ jira » (piège historique du
  lot #102) a été simplifiée en conséquence — elle n'a plus de raison d'être avec un seul
  sous-champ possible.
- Décision d'architecture « Frontière US/Jira » réécrite en « Frontière Jira : écarté » : le
  refus du MCP Atlassian (token, réseau en hook fail-open, dépendance externe) reste valable
  tel quel, seul le sujet change (pointeur retiré, pas seulement jamais connecté).

## Vérifié

- `node test/run-tests.js` : 1647 OK, 0 échec (suite complète, section `integrations.jira`
  supprimée, assertions `us`/export/fiche/trailer recentrées).
- `git grep -i jira` sur le dépôt : plus aucune occurrence fonctionnelle hors historique
  (CHANGELOG, fiches d'archive antérieures, `.vibe-agent/backlog.json`) et deux métaphores
  philosophiques inchangées (« un backlog n'est pas un Jira »).
- `integrations.command_optimizer` (RTK) : préservé et testé (start/done).

## Non vérifié

- Installation réelle non testée : le plugin installé (`~/.claude/plugins/cache/pmz-local/pmz/2.0.1`)
  reste une version antérieure, non republiée avec ce lot.

## Dette restante

- Aucune connue, propre à ce lot.

## Périmètre
(globs / fichiers touchés — noms seulement, jamais de diff)
- promptimizer/lib/backlog.js, promptimizer/scripts/backlog.js, promptimizer/lib/archive.js,
  promptimizer/scripts/close-batch.js, promptimizer/scripts/audit-batch.js
- ARCHITECTURE.md, README.md, CHANGELOG.md, test/run-tests.js

## Liens
- commit 9444709 · entrée CHANGELOG du 2026-07-28 · brut : archive/raw/lot-0105.md (local)
