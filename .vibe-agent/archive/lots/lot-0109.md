<!-- pmz:archive:lot 109 -->
# Lot #109 — Verdict chiffré de session fraîche dans close-batch

- epic : Reco de session fraîche · clos : 2026-07-28 · commit : 643f473 · session : inconnue
- verify : node test/run-tests.js → OK (1731 OK) · coût : non mesuré

## Objectif

Remplacer le jugement à l'œil « oui si le sujet change » (checklist de `/close-batch`) par un
verdict oui/non + raison chiffrée, fondé sur une donnée déjà mesurée (palier d'occupation), pour
que l'étape 7 de `close-batch.md` cesse de contredire la checklist par une reco fixe.

## Décisions (et pourquoi)

- Verdict fondé sur le palier d'occupation déjà PERSISTÉ par le hook Stop
  (`occupancy.stateFileFor(sessionId)`), pas une relecture du transcript : `/close-batch` n'est
  pas un hook, il n'a pas `transcript_path` — et rescanner le transcript coûterait du contexte.
- Seuil = `BUCKETS[1]` (300k) : repris tel quel du nudge subagent existant
  (`occupancy.evaluateSubagentNudge`, lot #52), déjà calibré et éprouvé en usage, plutôt
  qu'un seuil inventé au jugé pour ce lot.
- La priorité « après clôture » (lot ouvert, modifs non commitées) reste au-dessus du verdict :
  tant que la clôture n'est pas faite, la fraîcheur ne se juge pas.

## Vérifié

- `node test/run-tests.js` : 1731 OK, 0 échec.
- Verdict non/oui selon le palier persisté (bucket 1 vs bucket 2), raison chiffrée conforme.
- Priorité « après clôture » intacte sur lot ouvert (dirty tree).
- `close-batch.md` étape 7 renvoie au verdict (assertion testée).

## Non vérifié

- Comportement en session réelle longue (le palier n'est peuplé qu'après un premier Stop ;
  observé ici en repo de dev sans état d'occupation encore écrit → verdict "non" par défaut,
  raison explicite).

## Dette restante

Aucune identifiée pour ce lot.

## Périmètre
promptimizer/scripts/close-batch.js, promptimizer/commands/close-batch.md, test/run-tests.js

## Liens
- commit 643f473 · entrée CHANGELOG du 2026-07-28 · brut : archive/raw/lot-0109.md (local)
