---
description: Liste toutes les commandes Promptimizer disponibles avec leur description
allowed-tools: Bash(node *)
---

Affiche la liste des commandes Promptimizer disponibles.

Exécute : `node ~/.claude/promptimizer/scripts/help.js`

Restitue la liste telle quelle (dérivée des commandes réellement installées — sur le canal
plugin, `statusline` n'y figure pas, elle est réservée au canal manuel). N'invente aucune
commande absente de la sortie du script.
